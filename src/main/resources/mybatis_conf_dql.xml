<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!--引入 jdbc properties-->
    <properties resource="jdbc.properties"/>

    <!--设置对象别名，将来给 mapper 简化包路径熟悉呢-->
    <typeAliases>
        <typeAlias alias="User" type="com.mybatis.cn.base_dql_query.domain.User"/>
        <typeAlias alias="proxyUser" type="com.mybatis.cn.base_mapperproxy.domain.User"/>
    </typeAliases>

    <environments default="mysql">
        <environment id="mysql">
            <transactionManager type="JDBC"></transactionManager>
            <dataSource type="pooled">
                <property name="driver" value="${driver}"/>
                <property name="url" value="${url}"/>
                <property name="username" value="${username}"/>
                <property name="password" value="${password}"/>
            </dataSource>
        </environment>
    </environments>

    <!-- Continue editing here -->
    <mappers>
        <mapper class="com.mybatis.cn.base_dql_query.proxy.UserDao"/>
    </mappers>
</configuration>
